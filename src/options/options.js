// Options page script.
// TODO: load and persist settings via chrome.storage — sensitivity threshold,
// provider API key, and auto vs on-demand scan mode (see PROJECT_CONTEXT.md §6).
// Keys are stored locally and never hardcoded in the extension (§7).
