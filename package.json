{
  "name": "linkedin-content-validator",
  "version": "0.0.0",
  "private": true,
  "description": "Chrome extension that adds Community Notes-style AI-generation context cards to LinkedIn posts.",
  "type": "module",
  "scripts": {
    "lint": "eslint .",
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "test": "node --test",
    "package": "node scripts/package.mjs"
  },
  "engines": {
    "node": ">=20"
  },
  "devDependencies": {
    "@eslint/js": "^9.17.0",
    "eslint": "^9.17.0",
    "globals": "^15.14.0",
    "prettier": "^3.4.2"
  }
}
